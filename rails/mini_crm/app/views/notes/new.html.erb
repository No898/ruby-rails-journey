<% content_for :title, "New note" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New note</h1>

  <%= render "form", note: @note %>

  <%= render ButtonComponent.new(text: "Back to notes", href: notes_path, variant: :secondary, classes: "mt-4") %>
</div>
