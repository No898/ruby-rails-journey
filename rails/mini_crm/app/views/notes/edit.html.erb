<% content_for :title, "Edit note" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl mb-6">Edit note</h1>

  <%= render "form", note: @note %>

  <div class="mt-4">
    <%= link_to "Back to contact", contact_path(@note.contact), class: "text-blue-600 hover:underline" %>
  </div>
</div>
